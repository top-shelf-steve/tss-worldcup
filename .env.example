# Copy to .env and adjust as needed.

# Network
HOST=0.0.0.0          # 0.0.0.0 exposes the site to your LAN
PORT=8080

# How often (seconds) to re-pull the openfootball feed.
REFRESH_SECONDS=600

# --- Future, keyed live source (not used yet) -------------------------------
# Drop in server/sources/apifootball.py implementing DataSource, then set:
# API_FOOTBALL_KEY=
